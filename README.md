# CSC415-A1

CURL is a method for learning visual representations for reinforcement learning using contrastive learning. It learns representations by maximizing agreement between differently augmented views of the same observation using a contrastive loss (InfoNCE).

Requirements:

pip install torch 

pip install numpy

pip install torchvision

pip install kornia 
